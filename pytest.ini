[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Discover tests in all subdirectories: unit, integration, utilities, linting, benchmarks
addopts = 
    -p no:doctest
    --ignore=tests/fixtures
    --ignore=tests/benchmarks
